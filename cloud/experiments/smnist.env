# Default train.py overrides for smnist dispatches; the task config is authoritative.
ARGS="epochs=200 model.num_units=32 task.window_len=112 task.bptt_len=56 task.stretch_lo=1.0 task.stretch_hi=1.0"
