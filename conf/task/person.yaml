name: person
data_dir: ${paths.data_dir}/person
seq_len: 32
input_size: 7
output_size: 7
task_type: classification
per_timestep_labels: true
batch_size: 128

# ODE step sizing — model YAMLs interpolate via ${task.h} / ${task.ode_unfolds}.
h: 0.02
ode_unfolds: 1
