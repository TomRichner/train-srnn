name: smnist
data_dir: ${paths.data_dir}/smnist
seq_len: 28
input_size: 28
output_size: 10
task_type: classification
per_timestep_labels: false
batch_size: 128

# ODE step sizing — model YAMLs interpolate via ${task.h} / ${task.ode_unfolds}.
h: 0.02
ode_unfolds: 1
