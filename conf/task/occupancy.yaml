name: occupancy
data_dir: train_srnn/data/occupancy
seq_len: 16
input_size: 5
output_size: 2
task_type: classification
per_timestep_labels: true
batch_size: 128

# ODE step sizing — model YAMLs interpolate via ${task.h} / ${task.ode_unfolds}.
h: 0.02
ode_unfolds: 1
